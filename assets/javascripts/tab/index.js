export default function readJsonTabs(file) {
	console.log(file);

	// const response = await fetch(file);
	// const data = await response.json();
	// // generateHTML(data.hits);

	// console.log(data)
}

function createTabs() {}

function changeTabs() {}
